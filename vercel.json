{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": { "maxDuration": 300, "includeFiles": ["public/**", "data/**"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "src/index.ts" }]
}
